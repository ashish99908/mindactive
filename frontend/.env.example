# Base URL of the backend API.
# Local dev: leave unset (Vite proxy forwards /api to localhost:5000).
# Deployed (separate hosting): set to the backend's public URL, e.g.:
# VITE_API_URL=https://smritiloom-api.onrender.com/api
VITE_API_URL=
